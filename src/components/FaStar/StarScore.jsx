

export default function StarScore(){
    const scoreArray = [0, 1, 2, 3, 4];
    return(
        <div>
            {scoreArray.map((item, idx)=>{
 
                
            })}
        </div>
        )
}